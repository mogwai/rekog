{
  "name": "rekog-server",
  "version": "1.0.0",
  "description": "",
  "main": "src/",
  "scripts": {
    "start": "export NODE_PATH=./src && node ./src",
    "debug": "export NODE_PATH=./src && node --inspect-brk --use-strict ",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.224.1",
    "dotenv": "^5.0.1",
    "express": "^4.16.3",
    "multer": "^1.3.0"
  }
}
